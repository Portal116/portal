package Project;

import java.util.ArrayList;
import java.util.List;

import javax.swing.JFrame;
import javax.swing.JPanel;


public class Frame {

	   private static JFrame frame;
	//   private static JPanel homePanel = HomePanel.getPanel();
	//   private static JPanel menuPanel = MenuPanel.getPanel();
	//   private static JPanel memberPanel = MemberPanel.getPanel();
	//   private static JPanel orderPanel = OrderPanel.getPanel();
	//   private static JPanel productPanel = ProductPanel.getPanel();
	   private static List<JPanel> pList = new ArrayList<>();
	   
	   public static void main(String[] args) {
	      frame = new JFrame("¼îÇÎ¸ô °ü¸® ÇÁ·Î±×·¥");
	      frame.setVisible(true);
	      frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
	      frame.setResizable(false);
	      frame.setSize(1200, 800);
	      frame.setLocationRelativeTo(null);
	      frame.getContentPane().setLayout(null);

	      setPanel();
	      addPanel();
	      
	      frame.getContentPane().add(pList.get(0));
	      
//	      frame.getContentPane().add(homePanel);
//	      frame.getContentPane().add(menuPanel);
//	      frame.getContentPane().add(memberPanel);
//	      frame.getContentPane().add(orderPanel);
//	      frame.getContentPane().add(productPanel);

	   }
	   
	   public static void setPanel() {
//	      pList.add(HomePanel.getPanel());
//	      pList.add(MenuPanel.getPanel());
//	      pList.add(MemberPanel.getPanel());
	      pList.add(OrderPanel.getPanel());
//	      pList.add(ProductPanel.getPanel());
	   }
	   private static void addPanel() {
	      for(int i=0; i<pList.size();i++)
	         frame.getContentPane().add(pList.get(i));
	   }
	}