import java.util.*;

public class test {
	public static void main(String[] args) {
		int[] num = {3, 5, 0, 9, 4, 7, 8, 2, 11, 100};
//		List<Integer> numList = new ArrayList<Integer>();
		int searchNum = 6;
		List numList = Arrays.asList(num);
		numList.add(searchNum);
		
		while(true) {
			if(searchNum == num[i]) {
				System.out.println(num[i] + "을" + (i+1) + "번만에 찾았다.");
			}
		}		
	}
}
